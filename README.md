## Getting Started

```bash
# install dependencies for local development
npm i

# copy .env.example and setup the .env file
cp .env.example .env

# start setup for development
docker compose up -d
```
